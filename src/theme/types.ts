type Gradients = {
  horizontal: string;
  vertical: string;
};
export type Colors = {
  background: string,
  gradients: Gradients,
  color: string,
  border: string,
  btnBackground: string,
  iconBackground: string,
}